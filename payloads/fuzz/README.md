# General Fuzzing Payloads

These examples exercise common type, encoding, numeric, and length boundaries. They are small
enough for conservative manual testing and do not form a high-volume fuzzing campaign.

- Primary mapping: context-dependent
- Examples: 16
- Expected secure result: documented coercion or a stable `4xx` validation response
- File: `fuzz_safe_tests.payload`

Use only against an authorized test environment.
